export interface IProcedencia {
    id: number,
    procedencia: string,
    proc_otro_text: string | null,
    createdAt: string,
    updatedAt: string,
    deletedAt: string | null,
    SurveyId: number,
  }